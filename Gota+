<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Gota+</title>
  <link rel="stylesheet" href="style.css">
  <script src="main.js" defer></script>
</head>
<body>
  <h1>📺 Gota+</h1>
  <div id="catalogo"></div>
  <div id="playerContainer">
    <video id="videoPlayer" controls></video>
  </div>
</body>
</html>
body {
  font-family: Arial;
  background-color: #111;
  color: white;
  padding: 20px;
}

#catalogo {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.card {
  background-color: #222;
  border: 2px solid #0af;
  padding: 10px;
  width: 200px;
  cursor: pointer;
}

.card img {
  width: 100%;
}

#playerContainer {
  margin-top: 30px;
  display: none;
}
const catalogo = [
  {
    id: 1,
    titulo: "Episódio Gratuito",
    url: "https://meusvideos.com/ep1.mp4",
    tipo: "gratuito",
    thumbnail: "https://meusvideos.com/thumbs/ep1.jpg"
  },
  {
    id: 2,
    titulo: "Filme Premium",
    url: "https://meusvideos.com/filme-premium.mp4",
    tipo: "premium",
    thumbnail: "https://meusvideos.com/thumbs/premium.jpg"
  }
];

const catalogoDiv = document.getElementById("catalogo");
const videoPlayer = document.getElementById("videoPlayer");
const playerContainer = document.getElementById("playerContainer");

let premiumAtivo = false; // simula login premium

catalogo.forEach(video => {
  const div = document.createElement("div");
  div.className = "card";
  div.innerHTML = `
    <img src="${video.thumbnail}" alt="${video.titulo}">
    <h3>${video.titulo}</h3>
    <p>${video.tipo === "premium" ? "Premium 💎" : "Gratuito 🎬"}</p>
  `;
  div.onclick = () => {
    if (video.tipo === "premium" && !premiumAtivo) {
      alert("Esse conteúdo é Premium. Faça login.");
    } else {
      playerContainer.style.display = "block";
      videoPlayer.src = video.url;
      videoPlayer.play();
    }
  };
  catalogoDiv.appendChild(div);
});
<application>
  <id>com.gotaplus.tv</id>
  <title>Gota+</title>
  <vendor>SeuNome</vendor>
  <type>html</type>
  <main>index.html</main>
  <icon>icon.png</icon>
  <bgcolor>#000000</bgcolor>
  <requiredMemory>16</requiredMemory>
  <version>1.0</version>
</application>